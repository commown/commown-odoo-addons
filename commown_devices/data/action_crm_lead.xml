<odoo>

  <data>

    <record id="action_generate_picking" model="ir.actions.server">
      <field name="name">[commown] Generate picking</field>
      <field name="model_id" ref="model_crm_lead"/>
      <field name="sequence">5</field>
      <field name="state">code</field>
      <field name="code">action = record.action_generate_picking()</field>
    </record>

    <record id="action_menu_generate_picking" model="ir.values">
      <field name="name">[commown] Open Generate picking action</field>
      <field name="model">crm.lead</field>
      <field name="key">action</field>
      <field name="key2">client_action_multi</field>
      <field
          name="value"
          eval="'ir.actions.server,' + str(ref('action_generate_picking'))"/>
    </record>

  </data>

</odoo>
