<odoo>

  <record id="wizard_abstract_to_customer_form" model="ir.ui.view">
    <field name="name">[commown] Abstract to customer picking creation form view</field>
    <field name="model">abstract.to.customer.wizard</field>
    <field name="arch" type="xml">
      <form string="Send a device">
        <sheet>
          <div id="usage-title" class="h2">
            <field name="usage" class="oe_inline" />
          </div>
          <group>
            <field name="entity_id" invisible="1" />
            <field name="scheduled_date" />
            <field
                            name="products"
                            widget="many2many_tags"
                            options="{'no_create_edit': True, 'open': true}"
                            groups="stock.group_stock_manager"
                            readonly="0"
                            context="{'default_public': True}"
                        />
            <field name="carrier_account_id" />
            <field name="comment" />
          </group>
        </sheet>
        <footer>
          <button name="create_picking" string="OK" class="btn-primary" type="object" />
          or
          <button string="Cancel" class="btn-default" special="cancel" />
        </footer>
      </form>
    </field>
  </record>

  <record id="wizard_crm_lead_to_customer_form" model="ir.ui.view">
    <field
            name="name"
        >[commown] Customer picking creation form view from crm.lead</field>
    <field name="model">crm.lead.to.customer.wizard</field>
    <field name="inherit_id" ref="wizard_abstract_to_customer_form" />
    <field name="arch" type="xml">
      <field name="carrier_account_id" position="before">
        <field name="os" />
      </field>
    </field>
  </record>

</odoo>
