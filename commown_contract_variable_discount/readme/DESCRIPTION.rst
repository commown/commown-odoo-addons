This modules implements variable discount features that are specific to Commown:

- a discount when the user had no contractual issue (like device breakage) to date
- a discount when a coupon from a specific campaign was used during the order

It also adds the ability to use the commitment end date in the
discount validity condition.
