<?xml version="1.0" encoding="utf-8"?>
<odoo>

  <data>
    <record model="ir.ui.view" id="product_template_form_view">
        <field name="name">product.template.product.website.form</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="website_sale.product_template_form_view"/>
        <field name="arch" type="xml">

          <xpath expr="//field[@name='sale_ok']/ancestor::div[1]" position="before">
            <div>
              <field name="has_recurrent_payment"/>
              <label for="has_recurrent_payment"/>
            </div>
          </xpath>

          <!-- Improve labels for rental products -->
          <xpath expr="//group[@name='group_standard_price']" position="attributes">
            <attribute name="string">Price</attribute>
          </xpath>
          <xpath expr="//group[@name='group_standard_price']//field[@name='list_price']"
                 position="attributes">
            <attribute name="attrs">{"invisible": [("has_recurrent_payment", "=", True)]}</attribute>
          </xpath>
          <xpath expr="//group[@name='group_standard_price']//field[@name='list_price']"
                 position="after">
            <field name="list_price" t-if="has_recurrent_payment" string="Deposit"/>
          </xpath>
          <!-- END - Improve labels for rental products -->

          <xpath expr="//group[@name='group_standard_price']" position="after">
            <group name="rental" string="Rental price"
                   attrs="{'invisible':[('has_recurrent_payment','=',False)]}" >
              <field name="is_deposit"/>
              <field name="recurrent_payment_amount" widget="monetary" attrs="{'required': [('has_recurrent_payment', '=', True)]}"/>
              <field name="recurrent_payment_frequency"/>
              <field name="recurrent_payment_tax_ids" widget="many2many_tags"/>
            </group>
          </xpath>

        </field>
    </record>
  </data>

</odoo>
