<odoo>

  <record id="group_print_label" model="res.groups">
    <field name="name">Print shipping labels</field>
    <field name="implied_ids" eval="[(4, ref('base.group_user'))]" />
    <field name="comment">
      The user will be able to use wizards to print shipping labels.
    </field>
  </record>

</odoo>
