<odoo>

  <record id="contract_contract_customer_form_view" model="ir.ui.view">
    <field name="name">Contract form (in commown)</field>
    <field name="model">contract.contract</field>
    <field name="inherit_id" ref="contract.contract_contract_customer_form_view" />
    <field name="arch" type="xml">
      <xpath expr="//notebook" position="inside">
        <page name="payment_issues" string="Payment issues">
          <group name="payment_issues" string="Payment issues">
            <field name="payment_issue_ids" readonly="1">
              <tree default_order="create_date">
                <field name="name" />
                <field name="invoice_id" />
                <field name="stage_id" />
                <field name="invoice_unpaid_count" />
                <field
                                    name="timely_stage_change_datetime"
                                    string="Invoice next payment date"
                                />
                <field name="create_date" invisible="1" />
                <button
                                    name="get_formview_action"
                                    string="Ouvrir"
                                    type="object"
                                    class="btn-secondary"
                                />
              </tree>
            </field>
          </group>
        </page>
      </xpath>
    </field>
  </record>

</odoo>
