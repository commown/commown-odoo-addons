<odoo>

    <record id="action_contract_regenerate_forecasts" model="ir.actions.server">
      <field name="name">[commown] Regenerate contract forecasts</field>
      <field name="model_id" ref="contract.model_contract_contract"/>
      <field name="sequence">5</field>
      <field name="state">code</field>
      <field name="binding_model_id" ref="contract.model_contract_contract"/>
      <field name="code">records.action_regenerate_forecast()</field>
    </record>

</odoo>
